{
  "name": "dynamic-matrix",
  "version": "1.0.0",
  "description": "Dynamically generates GitHub Actions strategy matrices as JSON outputs",
  "main": "src/index.js",
  "scripts": {
    "build": "ncc build src/index.js -o dist --license licenses.txt"
  },
  "dependencies": {
    "@actions/core": "^1.10.1"
  }
}
